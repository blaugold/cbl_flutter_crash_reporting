name: cbl_flutter_crash_reporting
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  cbl: ^1.0.0-beta.10
  cbl_flutter: ^1.0.0-beta.9
  cbl_flutter_ce: ^1.0.0-beta.2
  sentry_flutter: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
